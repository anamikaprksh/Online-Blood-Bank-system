import React from 'react';
import AniLoading from '../static/animation.svg'

export default function ALoading(){
    return(
        <object type="image/svg+xml" data={AniLoading}>svg-animation</object>
    );
}